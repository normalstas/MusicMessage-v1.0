<UserControl x:Class="MusicMessage.UserCtrls.ChatsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:MusicMessage.UserCtrls"
             xmlns:conv="clr-namespace:MusicMessage.Converter"
             MinWidth="300" MinHeight="200">
    <UserControl.Resources>
        <conv:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Чаты" FontSize="16" FontWeight="Bold"/>
            <Button Content="+" Margin="10,0,0,0" Command="{Binding StartNewChatCommand}" 
                    ToolTip="Начать новый чат"/>
        </StackPanel>

        <!-- Список чатов -->
        <ListView x:Name="ChatsList" Grid.Row="1" 
          ItemsSource="{Binding Chats}" 
          SelectedItem="{Binding SelectedChat}"
          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction Command="{Binding OpenChatCommand}" 
              CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,2"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.Resources>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.Resources>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Padding="10" Margin="2" Background="White" CornerRadius="5"
                    HorizontalAlignment="Stretch">
                        <Grid HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Аватар -->
                            <Ellipse Grid.Column="0" Width="40" Height="40" Fill="LightBlue" 
                           Margin="0,0,10,0"/>

                            <!-- Информация о чате -->
                            <Grid Grid.Column="1" HorizontalAlignment="Stretch">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="{Binding OtherUserName}" 
             FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Grid.Row="1" Text="{Binding LastMessage}" 
             TextTrimming="CharacterEllipsis" Foreground="Gray"/>
                            </Grid>

                            <!-- Время и счетчик непрочитанных -->
                            <StackPanel Grid.Column="2" HorizontalAlignment="Right" Margin="10,0,0,0">
                                <TextBlock Text="{Binding LastMessageTime, StringFormat={}{0:HH:mm}}" 
                                 Foreground="Gray" FontSize="10"/>
                                <Border Visibility="{Binding UnreadCount, Converter={StaticResource IntToVisibilityConverter}}"
      Background="Red" CornerRadius="10" Width="20" Height="20" 
      HorizontalAlignment="Right" Margin="0,5,0,0">
                                    <TextBlock Text="{Binding UnreadCount}" Foreground="White" 
             HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Индикатор загрузки -->
        <ProgressBar Grid.Row="1" Height="5" IsIndeterminate="{Binding IsLoading}" 
                   VerticalAlignment="Top"/>
    </Grid>
</UserControl>